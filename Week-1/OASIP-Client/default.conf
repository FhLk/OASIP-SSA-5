upstream oasip_SVR {
    server oasip_SVR:8086;
}

server{
    listen 80;
    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    location ~ ^/api/.*$ {
        proxy_pass http://oasip_SVR;
    }
}